import HomeView from 'views/home'
//import App from 'app'

const controller = {
  'home': () => {
    console.log("called home route");
    //App.rootLayout.main.show(HomeView);
  }
}

export default controller